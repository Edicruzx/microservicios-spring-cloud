spring:
  application:
    name: authentication-server-oauth2

  cloud:
    config:
      uri: http://localhost:9000
      profile: desa
      enabled: true
logging:  #set logging console
  level:
    org.springframework.security: TRACE