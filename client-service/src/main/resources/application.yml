application:
  title: client-service
  version: 1.0.0
  autor: 'Mitocode'

spring:
  data:
    mongodb:
      database: mitocodedb
      uri: mongodb://localhost:27017/mitocodedb

management:
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans
