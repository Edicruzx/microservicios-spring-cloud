server:
  port: ${CUSTOM.SERVER.PORT:9004}

spring:
  application:
    name: user-service

  cloud:
    config:
      uri: http://localhost:9000
      username: root
      password: mitocode

  profiles:
    active: desa