server:
  port: ${USER_SERVER_PORT:9002}

spring:
  application:
    name: user-service

  cloud:
    config:
      uri: http://localhost:9000
      username: root
      password: mitocode

  profiles:
    active: desa
